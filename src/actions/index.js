const PingAction = require('./ping');
const PlayAction = require('./play');
const StopAction = require('./stop');
const SkipAction = require('./skip');
const CallAction = require('./call');

module.exports = {
  PingAction,
  PlayAction,
  StopAction,
  SkipAction,
  CallAction,
};
